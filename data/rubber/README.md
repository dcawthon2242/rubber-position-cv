This directory is populated by the pipeline (play IDs, clip manifests, frames, label packs,
measurements). It is git-ignored: frames are ~150 MB per 1,000 cells and Statcast pulls are
regenerated from Baseball Savant. See the top-level README for the run order.
